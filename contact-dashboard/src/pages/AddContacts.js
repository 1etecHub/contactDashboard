import React from 'react';
import AddContactForm from '../components/AddContactForm';

const AddContact = () => (
  <div>
    <h2 className="text-2xl font-bold text-blue-700 mb-4">Add Contact</h2>
    <AddContactForm />
  </div>
);

export default AddContact;
